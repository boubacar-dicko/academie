{% extends 'base.html.twig' %}

{% block title %}New Inscription{% endblock %}

{% block body %}
    <h1>Fiare une nouvel Inscription</h1>
    {% if is_granted('ROLE_ELEVE') %}
    {{ include('inscription/_form.html.twig') }}
    {% endif %}
    <!--a href="{{ path('inscription_index') }}">back to list</a-->
{% endblock %}
